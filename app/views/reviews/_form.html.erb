<%= simple_form_for [@restaurant @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit %>
<% end %>

<%= link_to "Go back", restaurants_path, class: "btn btn-primary"%>
